#pointDistance.jle
#by Ryan, Brice, and David

display "Calculates the distance between two points (enter x1, y1, x2, y2)", nl;
input x1, y1, x2, y2;

distance := ((x2-x1)^2 + (y2-y1)^2)^(1/2);
display "The distance between your two points is:", distance;